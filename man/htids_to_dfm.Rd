% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htid_to_dfm.R
\name{htids_to_dfm}
\alias{htids_to_dfm}
\title{Create a Quanteda Document-Feature Matrix from a Set of HTIDs}
\usage{
htids_to_dfm(
  htids,
  attempt_rsync = TRUE,
  dir = getOption("hathiTools.ef.dir"),
  pos_pattern = "NN|VB|JJ",
  include_pattern = "^\\\\p{L}+$",
  min_length = 3,
  page_language = "en",
  min_sentence_count = 3,
  to_lower = TRUE
)
}
\arguments{
\item{htids}{A character vector of Hathi Trust IDs, or a workset or data frame
with an \code{htid} column, as returned by \code{\link[=workset_builder]{workset_builder()}}.}

\item{attempt_rsync}{Whether to attempt to \code{\link[=rsync_from_hathi]{rsync_from_hathi()}} any requested
IDs whose JSON Extracted Features files are not already available locally in
\code{dir}. Defaults to \code{TRUE}. Requires an rsync client and network access.}

\item{dir}{The directory where the JSON Extracted Features files are cached.
Defaults to \code{getOption("hathiTools.ef.dir")}.}

\item{pos_pattern}{A regular expression matched against the part-of-speech tag
of each token; only tokens whose POS matches are retained. Defaults to
\code{"NN|VB|JJ"} (nouns, verbs, adjectives).}

\item{include_pattern}{A regular expression that each token must match to be
retained. Defaults to \code{"^\\\\p{L}+$"} (tokens made up solely of letters).}

\item{min_length}{The minimum token length (in characters) to retain.
Defaults to \code{3}.}

\item{page_language}{Only pages whose \code{calculatedLanguage} equals this value
are retained. Defaults to \code{"en"}.}

\item{min_sentence_count}{The minimum number of sentences a page must contain
to be retained. Defaults to \code{3}.}

\item{to_lower}{Whether to lower-case the resulting features. Defaults to
\code{TRUE}.}
}
\value{
A \link[quanteda:dfm]{quanteda::dfm} with one row per retained page and one column per
retained feature. Page-level metadata (sequence number, token count, etc.)
plus the \code{htid} is attached as document variables.
}
\description{
Reads the JSON Extracted Features files for a set of Hathi Trust IDs and
builds a single page-level \link[quanteda:dfm]{quanteda::dfm} whose documents are the individual
pages of the volumes and whose features are the (optionally lower-cased)
tokens selected via \code{pos_pattern}, \code{include_pattern}, and \code{min_length}.
}
\examples{
\dontrun{
htids <- workset_builder("democracy", pub_date = 1800:1810)
dfm <- htids_to_dfm(htids, dir = tempdir())
dfm
}
}
